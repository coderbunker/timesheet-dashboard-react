# timesheet-dashboard-react

## Deployment

We deploy by pushing the subdirectory public to the gh-pages branch (Github Pages):

```bash
git subtree push --prefix public/ origin gh-pages
```
